package com.manu.weathercontrol.features.weather.domain.rules

enum class RecommendationCode {

    // Ropa
    USE_JACKET,
    USE_COAT,
    WEAR_THERMAL_CLOTHING,
    WEAR_LIGHT_CLOTHING,
    USE_HAT,
    WEAR_SUN_GLASSES,
    WEAR_WATERPROOF_CLOTHING,

    // Comida
    EAT_WARM_MEALS,
    EAT_LIGHT_MEALS,
    AVOID_HEAVY_MEALS,
    STAY_HYDRATED,
    AVOID_COLD_DRINKS,

    // Actividad / comportamiento
    USE_SUNSCREEN,
    AVOID_SUN_EXPOSURE,
    USE_UMBRELLA,
    SECURE_OBJECTS,
    AVOID_HIGH_PLACES,
    AVOID_OUTDOORS
}

